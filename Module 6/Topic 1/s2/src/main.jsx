import React from "react";
import ReactDOM from "react-dom/client";
import GeminiAPISetup from "../GeminiAPISetup.jsx";
ReactDOM.createRoot(document.getElementById("root")).render(<GeminiAPISetup />);
